# Tasks

OCR 双执行模式的本地实现与离线验证已完成；5.4、7.1、7.2 仍需独立智能体或真实环境授权。遵循先观察失败测试、再实现、再回归的顺序；不把夹具测试或清单校验当作真实宿主/模型验收。

## 1. 协议与授权内核

- [x] 1.1 定义版本化请求、响应、授权范围、报告与错误协议；通过合法/缺字段/未知版本输入的契约测试验证，不接受未知状态为成功。
- [x] 1.2 实现 ASK、AUTO_REVIEW、MUTED 和逻辑提交任务生命周期；测试首次询问、重复回调去重、单次授权、会话自动审查、拒绝后跨回合静默。
- [x] 1.3 实现范围匹配和会话恢复；测试新会话、换仓库/worktree、换模型端点、扩大上下文范围不能复用授权，以及缺失会话 ID 的显式降级。
- [x] 1.4 实现单次手动审查、授权撤销和用户处置；测试静默下手动审查不解除静默、撤销后禁止新请求、报告建议不能触发自动修改或提交。

## 2. 状态与并发

- [x] 2.1 实现源码仓库外的私有状态存储、原子更新和版本冲突检查；通过临时目录集成测试验证权限、会话/仓库隔离、更新一致性和密钥不落盘。
- [x] 2.2 实现任务锁、运行租约与崩溃恢复；通过多进程测试验证同任务只执行一次、锁释放、迟到结果不能覆盖撤销状态和 abandoned 状态可重试。
- [x] 2.3 实现损坏状态恢复与任务级跳过、插件状态清理；测试损坏文件不授权、不无限阻塞，清理不触及源码或其他插件目录。

## 3. Git 内容与提交任务

- [x] 3.1 实现有界提交命令识别和不支持形式说明；测试普通 commit、引号中的说明文字、git -C、cd/暂存复合命令、-a、amend、pathspec、shell 包装与参数注入。
- [x] 3.2 实现候选索引与基线指纹；在真实临时 Git 仓库测试部分暂存、首次提交、删除/重命名、特殊文件名及 HEAD/索引变化导致报告过期。
- [x] 3.3 实现隔离 Git 快照与合成提交；测试原仓库 HEAD/索引字节/工作区不变，且隔离仓库仅包含候选版本、不执行项目 Hooks、过滤器和外部 diff。
- [x] 3.4 实现路径与规模保护；测试符号链接、子模块、冲突、越界路径、文件数/字节限制均返回明确原因，失败和取消后临时目录得到清理。
- [x] 3.5 实现提交成功确认和任务关闭；测试 Git 失败保留单次授权、非提交 PostToolUse 不关闭任务、成功提交之后下一任务恢复应有的询问/自动/静默行为。

## 4. OCR 适配与可信报告

- [x] 4.1 实现 OCR 能力探测、Delegation 默认范围及 OCR-managed 非敏感端点披露；测试过旧/缺失能力、执行模式变化和 doctor 不调用 LLM/不泄露凭据。
- [x] 4.2 实现授权后的双执行模式：delegate preview/rule 生成持久审查计划，managed 使用私有 `--output` 文件；验证参数、隔离、超时/取消、输出上限与清理。
- [x] 4.3 实现 delegate schema/宿主报告与 managed JSON 的严格归一化和覆盖核对；测试漏文件、重复文件、未知状态、全部过滤、异常/污染输出不成为成功。
- [x] 4.4 实现委托报告回填、内容复核、修复重审、保留风险继续和错误跳过；端到端测试模式/内容变化使旧计划和报告失效，跳过不生成 PASS。

## 5. 插件适配与智能体协作

- [x] 5.1 完成 CLI 子命令和三选一交互协议；通过进程级测试跑通未回答、仅本次、会话自动、会话静默和手动恢复的完整路径。
- [x] 5.2 实现 Hook 快速入口与五类事件适配；通过事件夹具验证不在 Hook 中运行模型或等待输入、提醒去重、静默后无主动提醒、成功提交才关闭任务。
- [x] 5.3 完成 Codex/ZCode/Kimi 清单及各自能力说明；分别验证文件路径、工具输入、返回协议与一致版本，不支持的能力明确降级，不能仅复制同一 JSON 当作适配完成。
- [ ] 5.4 编写插件专属 codereview 编排技能与本地技能声明；运行 skill-creator 校验，并以对话验收样例确认用户拒绝、报告包含指令和用户要求跳过时不会越权。
- [x] 5.5 定义 FlowGuard 可消费的证据格式并说明 CodeGuard 分工；通过消费者契约测试验证 skipped/failed/partial 不等于 accepted/pass，交付“不依赖 Hook 顺序”的集成说明，不修改另两个仓库。

## 6. 离线交付验证

- [x] 6.1 更新 README、架构图、隐私/外发说明、双模式依赖与安装/卸载文档；核对示例对应实际 CLI，明确本机旧版本与升级授权边界。
- [x] 6.2 重新运行完整测试、插件结构、技能及编译校验，记录双模式适配后的实际结果；不能以旧的 103 项结果作为新实现证据。
- [x] 6.3 更新三个规格的验收映射并运行 OpenSpec strict validation；保留真实 OCR/宿主未验证边界，仅在新行为和验证均完成后勾选。

## 7. 需要额外环境授权的运行验收

- [ ] 7.1 用户确认模型端点与测试代码外发范围后，用无敏感信息的临时仓库完成真实 OCR 审查；验证实际报告和提交内容一致。未获授权或环境不可用时保留未完成，不用夹具替代。
- [ ] 7.2 获得宿主安装授权后分别验证 Codex、ZCode、Kimi 的发现、加载和三种授权选择；记录各宿主真实版本和实测事件能力，缺失宿主保持 UNVERIFIED。

市场登记、远端创建、提交/推送、Release 与 tag 不属于本变更的隐式执行授权，待用户单独要求。真实运行任务未完成时不得归档为全部实现。
